// import { useTranslation } from "../../i18n";
import "../globals.css";

export default async function Page({ params: { lng } }) {
	// const { t } = await useTranslation(lng, "home");
	return (
		<main className='flex min-h-screen flex-col bg-[#172554] container mx-auto px-12 pt-16'>
			clients section soon
		</main>
	);
}
